# Session: 2026-06-22 — M4 Deployment & Bug Fixes

## What was done

### M4 Edge Functions — Deployed (37 total)
All 37 Edge Functions now deployed to Supabase project `hqiggiqwyxjiltltvoay`:
- **M1/M2 (10):** add-lifecycle-event, upload-document, generate-presigned-url, bulk-import-employees, update-employee, create-employee, access-revocation, exit-date-alert, future-joiner-activation, review-profile-edit
- **M3 (10):** check-in, check-out, log-wfh, auto-checkout, compute-attendance-status, manual-attendance, submit-regularization, review-regularization, late-mark-deduction, incomplete-attendance-reminder
- **M4 (17):** submit-leave, review-leave, cancel-leave, request-leave-cancellation, confirm-leave-cancellation, submit-comp-off, review-comp-off, opt-in-holiday, opt-out-holiday, upload-leave-attachment, sync-holiday-attendance, monthly-leave-accrual, year-end-leave-rollover, leave-sla-escalation, carry-forward-expiry-alert, carry-forward-lapse, comp-off-expiry-alert, comp-off-lapse

### Geofence hard-block
- `check-in` EF now blocks non-owner users who are outside any active geofence zone (403 FORBIDDEN)
- Location permission denial/timeout also blocked for non-owner (403 with "enable GPS" message)
- Owner always bypasses all checks

### Holiday CRUD fixes
- Empty state missing "Add Holiday" button — fixed by adding button to empty state card header
- Dialogs not rendering on empty state — consolidated three early returns into one
- `opt-in-holiday` missing `year` column in insert — fixed by adding `year` from holiday data
- `sync-holiday-attendance` EF created — syncs attendance records when holidays added/edited/deleted

### Calendar holiday display
- **TeamAttendancePage** — now queries `holidays` table and shows gray holiday cells for future dates
- **AttendanceCalendar** (employee personal) — fetches holidays for displayed month, renders gray cells for future holiday dates

### Leave attachment upload
- New EF `upload-leave-attachment` — validates file (PDF/JPEG/PNG, 5MB max), uploads to `employee-documents` bucket under `leave-attachments/` prefix, returns storage_path
- `ApplyLeaveForm` — added "Attach File" button with file picker, upload progress, filename display, clear button
- Attachment is uploaded first, then storage_path sent with the leave application

### Deploy fixes
- `submit-leave`, `compute-attendance-status`, `incomplete-attendance-reminder` were missing from deployed functions despite deploy command returning success — redeployed all three

### Bug: Leave not showing in admin approval queue
User reports leave submits successfully but doesn't appear in PendingLeaveQueue on admin pages.

## Next Session — To Do

### Bug: Leave approval queue not showing
- Investigate `PendingLeaveQueue` component — likely query filter issue
- Check `fetchPendingLeaveApplications` in `src/features/leave/api.ts` — filters by `status = 'pending'`
- Check RLS policies on `leave_applications` table — admin might not have SELECT access
- Check `RoleGuard` on `TeamLeavePage` route

### M5 — Reports Module
- **4 stub report pages:** ReportsAttendancePage, ReportsHeatmapPage, ReportsHeadcountPage, ReportsRegularizationPage
- **Empty feature layer:** `src/features/reports/` — api.ts, hooks.ts, utils.ts all `TODO: M5`
- **Missing reports home page:** `/reports` route with role-based cards

### Stub Pages & Edge Functions
- `SettingsNotificationsPage` — 8-line placeholder
- `probation-end-alert` EF — returns `{ processed: 0 }`, needs implementation (BR-EMP-07)
- `update-app-config` EF — no key validation or upsert logic

### Infrastructure
- **17 cron functions not scheduled** in Supabase Dashboard (10 M3 + 7 M4)
- **RESEND_API_KEY not configured** — welcome/notification emails fail silently
- **Reference data deleted** — departments, designations, shifts need re-seeding via UI
- **Migration naming conflict** — two `0010_*` files, run `supabase migration repair`
- **origin/main on GitHub** reverted to pre-scaffold state
